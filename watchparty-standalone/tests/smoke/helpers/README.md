# Smoke Test Helpers

Shared helpers belong here only when they are genuinely reusable across test layers.

Keep server protocol helpers separate from Playwright/browser helpers where possible so deterministic tests remain lightweight and understandable.